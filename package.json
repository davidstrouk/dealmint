{
  "name": "dealmint",
  "version": "1.0.0",
  "private": true,
  "description": "Negotiate, mint the mandate, and settle anywhere.",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "start": "pnpm --filter web start",
    "db:generate": "pnpm --filter @dealmint/prisma generate",
    "db:push": "pnpm --filter @dealmint/prisma push",
    "db:seed": "pnpm --filter @dealmint/prisma seed",
    "db:studio": "pnpm --filter @dealmint/prisma studio",
    "lint": "pnpm --filter web lint",
    "type-check": "pnpm -r type-check"
  },
  "keywords": ["dealmint", "ethereum", "pyusd", "avail", "negotiation"],
  "license": "MIT"
}

